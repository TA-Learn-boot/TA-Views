# qa | staging — must match a key under environments in public/config/gateway.yaml
# Used by vite.config.js proxy target and by production builds for the API base URL.
VITE_APP_ENV=qa

# Optional: milliseconds to wait after successful login before navigating (default 800).
# VITE_POST_LOGIN_DELAY_MS=1200
